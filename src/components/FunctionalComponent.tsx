import * as React from 'react';

type FunctionalComponentProps = {};

export const FunctionalComponent = (props: FunctionalComponentProps) => (
  <h1 style={{ fontSize: 22, color: 'blue' }}>Functional Component</h1>
);
