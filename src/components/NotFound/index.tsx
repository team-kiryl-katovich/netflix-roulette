import * as React from 'react';

import { Label } from '@common/components';

import './styles';

export const NotFound = () => (
  <div className="page-not-found">
    <Label className="page-not-found__label" text="Page not found" />
  </div>
);
